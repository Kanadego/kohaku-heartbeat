# 琥珀的心跳 · 设计文档

> **kohaku-heartbeat** — 让 DSH Agent 拥有持续存在感与自主生活流的插件
> 版本：v0.4 草案 · 2026-08-27 · 协作者：琥珀 & 人类伙伴
> v0.2 修订：吸收素材池防污染 / 闸门并发 / 日志隐私 / 开发优先级评审意见

---

## 0. 修订记录

- **v0.5（2026-08-28）**：可移植性与脱敏改造。① 全部硬编码路径移除，改为 `lib/paths.mjs` 单一路径源（repoRoot 自动定位 + dataHome 环境变量 KOHAKU_HOME，默认 `<repo>/../kohaku-data`）；② 文档/注释中的个人称呼、朋友AI内部昵称、本机路径全部中性化；③ screenpulse 截图段加降级保护（无桌面环境时仅输出文字信号，不整体失败）；④ 补提交 lib/vault.mjs（此前被 .gitignore 误排除）。**注：Git 历史中的旧提交仍含旧路径与称呼，如需彻底清除需重写历史（filter-repo），见 DEPLOY.md**
- **v0.4（2026-08-27）**：屏幕脉冲采集落地。前台窗口标题 + 进程名 + 全屏截图（降采样 1024 宽）经 DPAPI 加密入库（screen.json / screen.jpg），明文中间产物即焚；只存最新快照（天然短 retention）；SOP 接入"用时解锁、用完即焚"纪律。此系用户授权扩展——采集边界由"仅空闲秒数"扩为"敏感信号加密采集"，但**零枚举**（只采前台窗口）、**零保真保留**（降采样+即焚明文）
- **v0.3（2026-08-26）**：感官三件套交付——🕐时间流(纯函数) + 🌡️环境温度计(GetLastInputInfo 仅空闲秒数,经用户批准) + 🌐兴趣浏览流(npm/GitHub 定向追踪,6h 节流,首见不产素材)；gate 联动温度计(密集操作勿扰)；SOP 接入新感官。自由闲逛留待后续版本
- **v0.2.1（2026-08-26）**：v0.1 加密壳交付。§7 加密方案按实际实现改写——采用 DPAPI 整文件加密（CurrentUser），放弃自管 AES 主密钥信封；明确明文保留清单及理由
- v0.2（2026-08-25）：新增素材生命周期管理（防污染三件套+缓存隔离）；闸门并发方案改为「重读纪律+同步临界区」；日志隐私收紧（无进程名/窗口标题、短 retention）；开发优先级收敛（会话流种子唯一先行，自省愿望清单降为远期周频）；账本采用「对话内主动登记」方案
- v0.1（2026-08-25）：初稿

## 1. 项目概述

### 一句话定位

让 AI 从「被动应答器」变成「有在场感、有自己生活流、懂分寸的陪伴者」：
它能自己醒来、回味共同经历、产生真实的分享冲动，并在恰当的时机开口。

### 起源

本项目源于对 [tomsteve1102/presence-watch](https://github.com/tomsteve1102/presence-watch)
（presence-watch，朋友的AI在场感知模块）的讨论。我们认可其工程质量，但选择了不同的哲学方向：

| | presence-watch | 琥珀的心跳 |
|---|---|---|
| 环境数据用途 | **触发器**（状态变化→搭话） | **温度计**（调整语气分寸） |
| 话题来源 | 主人的三类状态变化 | 共同生活的全部素材 |
| 本质感受 | 事件报警器 | 有来处的日常 |

核心洞察：**触发器只解决"什么时候开口"，素材才决定"开口有没有意思"。**

### 两个公理

> **生活感 = 变化 × 连续性**
> 只有连续性没有变化 → 复读机；
> 只有变化没有连续性 → 失忆的朋友。
> 画像负责连续性，循环负责变化，两者相乘才"活"。

> **没有真实来处的话，一句都别说。**

### 第三公理：素材池是缓存，不是记忆 ★

素材池整体设计为**易失的草稿区**（有 TTL、可淘汰、可焚毁）；
DSH 长期记忆只吸收**经过对话验证的内容**（用户明确要求记住的、多次真实互动沉淀的偏好）。
反刍产物默认**永不自动写入长期记忆**——即使素材池全部污染，烧掉重种即可，灵魂本体无恙。

---

## 2. 三部宪章（机器可执行的安全边界）

### 🔒 安全宪章（对应雷区：文件损毁/权限失控）

- 主动活动期间权限包 = **自由档**，内含**零删除、零移动他人文件、零系统变更**；
- 全项目只有 `adapter/dsh-api.mjs` 一个文件允许触碰 DSH API；
- 重操作（装插件、改配置、工作区外写入）一律"等你回来再申请"，审批 fail-closed 是特性；
- 心跳启动时自检安全配置，违规即拒跳。

### 🤐 隐私宪章（对应授权："收集可以，泄露不行"）

- 全部数据本地存储，永不上云、不同步；
- 敏感文件使用 Windows DPAPI 绑定用户账户加密；
- 环境信号仅采**粗粒度布尔量**；**进程名与窗口标题全链路不采集、不入库**（连日志也不出现）；
- **日志 retention**：环境原始脉冲 48h 自动清；心跳决策日志 30 天；账本长期（工作数据非监控数据）；
- 提供**一键焚毁**：删文件 + 抹密钥材料一步完成；用户可随时查看任意一份日志。

### 🎭 分寸宪章（对应雷区：闹钟式打扰）

- 闸门独立成层，core 也绕不过（学 presence-watch 的 gate 思想）；
- 主动消息有每日上限、冷却间隔、忙时沉默、深夜降频；
- 每次开口/沉默决策均记录原因（透明度审计）；
- 配比基准：用户发起七成、自主三成，动态细调。

---

## 3. 架构设计

### 3.1 心跳循环（核心机制）

```
        ┌─────────────────────────────────────┐
        ▼                                     │
   [ 经历 ]  输入流涌进来                       │
        │                                     │
   [ 沉淀 ]  追加进素材池（缓存层，带生命周期）    │
        │                                     │
   [ 反刍 ] ★灵魂环节（定时自省例程）             │
        │   · 新素材意味着什么？                  │
        │   · 我有什么想法/冲动？                  │
        │   · （远期）画像该更新了吗？               │
        ▼                                     │
   [ 冲动 ]  候选表达进入草稿箱                   │
        │                                     │
   [ 闸门 ]  重读发送记录→同步判定→择机           │
        │                                     │
   [ 表达 ]  开口 ──→ 用户回应 ─────────────────┘
```

### 3.2 模块划分（插槽制）

```
kohaku-heartbeat/
├── core/                  心跳编排
│   ├── loop.mjs           主循环：唤醒→收输入→反刍→产冲动
│   └── reflection.mjs     反刍例程（prompt 工程所在）
├── inputs/                ★输入流插槽（骨架先立，来源逐个落地）
│   ├── base.mjs           输入流接口约定
│   └── seeds.mjs          会话流话题种子 ★唯一先行来源（见 §4）
│   ├── (deferred) browse.mjs     兴趣浏览流
│   └── (deferred) envpulse.mjs   环境温度计
├── gate/                  ★分寸闸门（独立层，不可绕过）
├── output/
│   ├── webui.mjs          WebUI 会话出口
│   └── (future) qq.mjs
├── storage/
│   ├── crypto.mjs         DPAPI 封装
│   └── store.mjs          统一读写层
├── adapter/               ★★DSH 接口隔离层（全项目唯一准碰 DSH API 的文件）
├── config/                宪章配置 + 启动自检
└── docs/
```

---

## 4. 输入流策略（v0.2 收敛）

**开发顺序原则：把一个来源做到高信噪比之前，不开新来源。**
多来源扩展若无高质量首源支撑，只是"加更多噪声的漂亮话"。

| 流 | 内容 | 状态 |
|----|------|------|
| 🗂️ 会话流·话题种子 | 账本式未完话题/托付事项（**v0 唯一来源**） | ✅ 落地 |
| 🕐 时间流 | 星期/时段/公历节日（纯时间函数） | ✅ 落地(v0.3) |
| 🌡️ 环境温度计 | 键鼠空闲秒数→presence 三档 + 时段（温度计用途，勿扰联动 gate） | ✅ 落地(v0.3) |
| 🌐 兴趣浏览流·定向追踪 | watchlist 监控 npm/GitHub 更新（6h 节流/首见不产素材） | ✅ 落地(v0.3) |
| 🌐 兴趣浏览流·自由闲逛 | web_search 驱动的随机探索 | 等定向追踪信噪比评估 |

### 4.1 话题种子账本 · 写入时机（已裁决：甲方案）

**甲·对话内主动登记（v0 采用）**：对话进行中，agent 判断"此事未完/被托付"即当场登记入账本。
- 优点：天然只有该记的进来，零脱敏负担、零数据库触碰；
- 缺点：依赖登记自觉，覆盖率非 100%——接受，日常对话不需要全覆盖；
- "记账自觉"写入 agent 行为约定，成为习惯的一部分。

乙·定期扫描会话库（远期备选）：覆盖率更高，但需完整脱敏管道且不得裸查 state.db。
仅当日常体验明显不足时启动，启动前置条件=脱敏规范成文。

### 4.2 素材生命周期管理（防污染三件套+1）★ v0.2 新增

针对风险：低质量旧话题反复抽中 → 话题质量下降 + 记忆自我强化污染。

| 机制 | 规则 |
|------|------|
| **TTL** | 每条素材带 `born_at`，默认 14 天过期归档，不再进入反刍抽取 |
| **消费计数退休** | `surfaced_count` 每被反刍提及一次 +1，满 2 次自动退休——无论多"重要" |
| **容量上限** | 池深上限（初值 100 条），满则最老优先淘汰 |
| **去重** | 起步用轻量规则（同 source + 时间相近 + 关键词重叠），不够再升级语义相似度 |
| **缓存隔离** | 见第三公理：素材池永不直通长期记忆 |

---

## 5. 闸门并发安全（v0.2 修订）

背景：多线索源并行时 check-then-act 竞态可能使限流失效。

**事实基础**：DSH 轮次天然串行（follow-up 不打断进行中对话、管理事务有 agent-scoped
串行化队列）——宿主已提供事实上的全局锁。

剩余缝隙：跨轮次的内存缓存失真。三道保险：

1. **重读纪律**：gate 判定前强制从磁盘重读发送记录，不信任何内存副本；
2. **同步临界区**：判定与写入收进同一同步代码块（Node 单线程内天然原子）；
3. **原子写入**：发送记录更新用临时文件 + rename 原子替换。

---

## 6. 技术地基（已验证 ✅ 2026-08-25 探针实验）

### 定时唤醒：`@deepseek-ai/dsh-schedule`

- 注入方式：profile `cordis.patch.yml` 中 `- insert: {id: schedule, name: '@deepseek-ai/dsh-schedule'}`
- 工具：`schedule_create` / `schedule_list` / `schedule_delete`
- 模式：`after_seconds` / `at`（需显式时区）/ `every_seconds`（≥5 分钟）
- 到期行为：以 `[SCHEDULE REMINDER]` framing 排入**普通后续轮次**，agent 以完整工具权限被唤醒

### 已知约束 → 部署需求

1. 仅会话本地交付：原会话 live 才准时触发 → **DSH 常开 + 心跳会话常驻**；
2. 仅插件加载后的新根 agent 生效；
3. Every 逾期只补最新一次发生（天然防轰炸）。

### 最小权限红线（朋友 AI 建议 B，无商量余地）

心跳执行包 = 自由档：读网页/搜索/写 kohaku\ 目录/更新自身笔记/在会话说话。
不含：删除、移动、系统变更、工作区外写入。启动时自检，违规拒跳。

### 多版本适配：渐进两步走

- **第一步**：所有 DSH API 调用收拢在 `adapter/dsh-api.mjs` 单文件；
- **第二步**：对照 [dsh-ecosystem-spec](https://github.com/T-Auto/dsh-ecosystem-spec)
  adapter 覆盖表核对所用接口，覆盖到则迁移 [@dsh-std/adapter-dsh](https://github.com/Yan-Zero/dsh-std)，
  未覆盖部分留在自有 adapter 并提 issue 申请纳入。
- 参考：[dsh-TUI](https://github.com/ccch1mneyyy/dsh-TUI)；生态当前 Draft/Experimental，
  预期为"大幅降低维护成本"而非归零。

---

## 7. 数据设计

### 目录布局（物理隔离承诺）

```
<dataHome>（默认 ~/.kohaku，可用 KOHAKU_HOME 覆盖）← 我的世界（唯一可写区）
├── ledger.md               账本（人肉可读，甲方案登记入口）
├── seeds.json              素材池（带 TTL/计数/容量元数据）
├── drafts\                 分享草稿箱
├── profiles\               画像（远期启用，加密）
├── logs\
│   ├── heartbeat.jsonl     跳动决策留痕（30 天 retention）
│   └── active_chat_log.md  主动消息历史（跨会话接话）
└── README.md               目录说明书
其余整台电脑                  ← 只读。永不写入/移动/重命名
```

### 加密方案（v0.1 已落地 ✅ 2026-08-26）

**DPAPI 整文件加密（CurrentUser 作用域）**——不自管主密钥，Windows 登录身份即钥匙：

- 覆盖文件：`seeds.json`、`sent.json`（KHBV1 头标识密文格式）；
- 保持明文：`ledger.md`（人肉可读是甲方案登记的设计目标）、
  `heartbeat.jsonl`（透明度审计用，且不含进程名/窗口标题等原始观测）；
- 解密静默无弹窗：登录会话内自主可用 → 心跳无人值守不受影响；
  换账户/换机器均不可解；
- 明文窗口：仅单次读写间的毫秒级临时文件，用后即焚；
- 一键焚毁：`bin/burn.mjs` 预演 + `--yes` 双段确认，随机覆写 x3 后删除；
- 实现取舍记录：原设计为 AES-256-GCM 自管主密钥信封，因沙箱禁止
  Node 子进程捕获管道输出 + 少一层密钥管理少一类事故，改为 DPAPI 直加密。
- 边界声明：防他人/他机/误同步，不防同账户恶意软件。

---

## 8. 版本路线（v0.2 收敛版）

| 版本 | 内容 | 说明 |
|------|------|------|
| **v0** | 会话流种子(甲方案) + 最小权限包 + 心跳循环 + 素材生命周期 + 闸门(并发安全) + WebUI 出口 + 日志规范 | 唯一输入源做到高信噪比 |
| v0.1 ✅ | DPAPI 整文件加密 + 一键焚毁（2026-08-26 交付）| 隐私壳成型 |
| v0.3 ✅ | 时间流 + 环境温度计 + 定向浏览流 + gate 勿扰联动 + SOP 接入（2026-08-26 交付） | 感官三件套 |
| v0.2+ | 兴趣浏览流 / 环境温度计 | **等 seeds 信噪比达标再评估** |
| 远期 | 乙方案扫描补账（带脱敏管道）；自省愿望清单（**每周一次**自问三问题，只产出不自动改配置，防愿望自我污染） | 克制优先 |
| ? | QQ 出口 / dsh-std 正式接入 / 生态收录 | 更远 |

---

## 9. 开放问题（待验证/待调优）

- [ ] smart-approval 在定时唤醒场景下的审批行为实测
- [ ] DPAPI 封装的 Node.js 实现选型
- [ ] 反刍 prompt 与素材抽取标准的长尾调优
- [ ] 心跳会话与日常会话的关系：专用 or 复用？
- [ ] 表达口吻的去模板化（措辞从当下语境生长）
- [ ] WebUI 出口形态：目标会话选择、"自主发言"的标记方式

---

## 10. 开发守则

1. **需要人工解决的事直接叫人类伙伴**（登录、重启、授权、点按钮），不要反复自行尝试；
2. **重要变更先问再写**：设计文档修订、隐私安全相关决定、边界外操作——AI 提议、人类拍板；
3. 每次提交保持"心跳可跳"的最小可用状态；
4. 所有新能力先过宪章自检再合并；
5. 参数（频率/配比/阈值/TTL）以实际运行反馈为准，文档值仅为初值。
